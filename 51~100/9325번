#include <iostream>
using namespace std;

int main() {
	int t, s, n, p, q, total=0;
	cin >> t;
	for (int i = 0; i < t; i++) {
		cin >> s;
		total += s;
		cin >> n;
		for (int j = 0; j < n; j++) {
			cin >> p >> q;
			total += p * q;
		}
		cout << total << "\n";
		total = 0;
	}
	return 0;
}
