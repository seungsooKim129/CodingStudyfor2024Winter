#include <iostream>
using namespace std;

int main() {
	int n, total=0, std, apple;
	cin >> n;
	for (int i = 0; i < n; i++) {
		cin >> std >> apple;
		total += apple % std;
	}
	cout << total;	
	return 0;
}
