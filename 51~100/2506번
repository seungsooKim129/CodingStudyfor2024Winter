#include <iostream>
using namespace std;
int main() {
    int n, x, cnt=0, total=0; 
    cin >> n;
    for (int i = 0; i < n; i++) {
        cin >> x;
        if (x == 1) {
            cnt++;
            total += cnt;
        }
        else cnt = 0;
    }
    cout << total;
}
