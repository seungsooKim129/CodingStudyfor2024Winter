#include <iostream>
#include <string>
using namespace std;
int main() {
    int N, max=-1000001, min=1000001, n;
    cin >> N;
    for (int i = 0; i < N; i++) {
        cin >> n;
        if (n > max)max = n;
        if (n < min)min = n;
    }
    cout << min << " " << max;

}
