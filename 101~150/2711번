#include <iostream>
#include <string>
using namespace std;
int main() {
    int T, l;  
    string s;
    cin >> T;
    for (int i = 0; i < T; i++) {
        cin >> l >> s;
        for (int j = 0; j<s.length(); j++) {
            if(j!=l-1)cout << s[j];
        }
        cout << "\n";
    }
    
}
